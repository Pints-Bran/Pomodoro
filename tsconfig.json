{
  // Solution file: every source file belongs to exactly one project below, so
  // editors resolve each file against the options it is actually built with.
  "files": [],
  "references": [
    { "path": "./tsconfig.app.json" },
    { "path": "./tsconfig.worker.json" },
    { "path": "./tsconfig.tools.json" }
  ]
}
